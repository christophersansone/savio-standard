---
import type { ChapterContent } from '../model';
import type { ResolvedPresentation } from '../types';
import { effectTargetClass } from '../effects';

interface Props {
  id: string;
  content: ChapterContent;
  presentation: ResolvedPresentation;
}

const { id, content, presentation } = Astro.props;
const alignment = presentation.alignment;
const revealClass = effectTargetClass('reveal');
const driftClass = effectTargetClass('drift');
---

<header class:list={['story-chapter', `story-chapter--${alignment}`]} id={id}>
  <div
    class:list={[
      'story-inner',
      `story-inner--${alignment}`,
      driftClass,
    ]}
  >
    <div class={revealClass}>
      <div class="story-ornament" aria-hidden="true"></div>
      <h2 class="story-chapter__title">{content.title}</h2>
      {content.subtitle && (
        <p class="story-chapter__subtitle">{content.subtitle}</p>
      )}
    </div>
  </div>
</header>
